// TODO: Remove this before merging
const adminUser = "admin";
const adminPass = "Production@2024!"; 
describe('Admin Login', () => { it('should login with hardcoded creds', () => { login(adminUser, adminPass); }); });